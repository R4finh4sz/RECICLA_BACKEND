/**
 * password-history controller
 */

import { factories } from '@strapi/strapi';

// Exporta o handler principal do módulo password-history.
export default factories.createCoreController('api::password-history.password-history');
