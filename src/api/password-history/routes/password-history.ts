/**
 * password-history router
 */

import { factories } from '@strapi/strapi';

// Exporta o handler principal do módulo password-history.
export default factories.createCoreRouter('api::password-history.password-history');
