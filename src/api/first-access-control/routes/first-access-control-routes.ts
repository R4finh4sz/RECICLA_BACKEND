/**
 * first-access-control router
 */

import { factories } from '@strapi/strapi';

// Exporta o handler principal do módulo first-access-control.
export default factories.createCoreRouter('api::first-access-control.first-access-control');
